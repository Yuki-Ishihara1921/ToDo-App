<p id="notice"><%= notice %></p>

<p>
    <strong>ToDo:</strong>
    <table>
        <tbody>
            <tr>
                <td>Title</td>
                <td><%= @todo.title %></td>
            </tr>
            <tr>
                <td>Detail</td>
                <td><%= @todo.detail %></td>
            </tr>
        </tbody>
    </table>
</p>

<%= link_to 'Edit', edit_todo_path(@todo) %>
<%= link_to 'Back', root_path %>